# 20260910-0925-release-v0-3-2 Release v0.3.2

- **status**: completed
- **priority**: P1
- **owner**: release/session-20260910
- **createdAt**: 2026-09-10 09:25

## Description

Publish the verified startup-lock fixes as v0.3.2, including platform archives, publisher signatures, and checksums required by lode-cli self-update.

## ActiveForm

Published and verified the v0.3.2 patch release.

## Dependencies

- **blocked by**: (none)
- **blocks**: (none)

## Notes

- Full tier: the release spans workspace versions, lockfile, release notes, and documentation references. The user explicitly authorized publishing the new version, including the necessary commits and push.
- The current release is v0.3.1, origin/main matches the local base, and repository administration access is available.
- LODE_SIGNING_KEY is configured for the release workflow; no key material was read.
- The implementation was verified by 246 workspace tests, 4 doctests, and 7 related end-to-end tests, plus formatting and Clippy. See the completed lock tasks.
- Plan: ../plan/20260910-0925-release-v0-3-2.md.

## Release Preparation

- Workspace version, path dependency versions, local package lockfile entries, changelog, and README release-tag references now target v0.3.2. No third-party dependency changed.
- Remote main is unprotected and matched e0de42ca8d131a09d29baf9b4d23997ac9f6d885 before preparation.
- Before the push, GitHub reported the CI workflow as deleted while Release and docker workflows were active. The push reactivated general CI without changing repository workflow settings.
- Repository secrets include LODE_SIGNING_KEY. Docker credentials are not listed at repository level; organization-secret metadata is inaccessible (HTTP 403), so inherited Docker credentials cannot be confirmed in advance.

## Pre-publication Verification

- cargo fmt and workspace Clippy passed.
- Workspace nextest passed 246 tests (one standalone subprocess helper skipped); all 4 doctests passed.
- All 43 non-Docker end-to-end tests passed across 29 files.
- cargo build --release --locked passed; target/release/lode --version reports 0.3.2.
- Changed-file spelling and git diff --check passed. The lockfile differs only in the three local package versions.
- Local review: PASS, no new actionable findings. The prior cargo-shear findings remain unrelated to this release and no dependency changes were introduced.
- Downloaded the previous v0.3.1 Linux x64 release for isolated self-update verification; its SHA256 matches the published asset digest.

## Publication Results

- Pushed release commit f9a03af4a66319c9112c3c807c628075cbddc9e4 to origin/main.
- Published https://github.com/dotns/lode/releases/tag/v0.3.2 as the latest stable release, targeting that exact commit.
- Release workflow [34461115840](https://github.com/dotns/lode/actions/runs/34461115840) passed all four platform builds and the upload job.
- All nine assets are uploaded: Linux x64/arm64 and macOS x64/arm64 archives, their four signatures, and SHA256SUMS. Downloaded every asset; all archive checksums and publisher signatures passed verification against the built-in release keys.
- General CI exposed a pre-existing spelling false positive in crates/lode/release-keys.txt. Added a file-specific spelling exclusion without changing any key material; the full local typos check now passes. This documentation/tooling follow-up does not change the tagged release binary code.
- Follow-up commit 3ed181659ddb64f08f2e3f16c3a6056235a23212 fixes the encoded-key spelling false positive; its CI run [34461452417](https://github.com/dotns/lode/actions/runs/34461452417) passed all seven jobs, including the end-to-end suite.
- Real container restart smoke passed using the v0.3.2 release build: seed an offline service, start it with a persistent data directory, SIGKILL the container (exit 137), confirm PID metadata remains, restart the same container, and verify the service returns to running while preserving the advisory lock inode. Test containers were removed.
- An isolated copy of the published v0.3.1 Linux x64 binary successfully ran lode-cli self-update against the latest GitHub release; the resulting binary reports lode 0.3.2. The first download failed with "Peer disconnected" and retained v0.3.1; one retry succeeded with signature verification enabled.
- GitHub confirms v0.3.2 is the latest stable release at f9a03af4a66319c9112c3c807c628075cbddc9e4. Docker Hub image publication was not dispatched because publishing credentials could not be confirmed; this release delivers the signed GitHub archives.

- complete: Published v0.3.2 with verified signed assets, successful self-update and forced-container-restart checks, and passing CI.
