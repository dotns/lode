# 20260910-0925-release-v0-3-2 Release v0.3.2

- **status**: implementing
- **createdAt**: 2026-09-10 09:25
- **approvedAt**: 2026-09-10 09:25
- **relatedTask**: 20260910-0925-release-v0-3-2

## Context

v0.3.1 is the current release. The working tree contains verified PID identity checks and persistent flock ownership. The Release workflow builds Linux x64/arm64 and macOS x64/arm64 archives, signs them, and uploads SHA256SUMS. Publishing a GitHub Release triggers that workflow. Docker packaging is a separate manual workflow.

## Proposal

1. Bump the workspace and local crate dependency versions to 0.3.2, refresh only the local package versions in Cargo.lock, and update the changelog and README tag examples. Verify version consistency and locked builds.
2. Review and commit the scoped changes, push main, and verify CI on the release commit.
3. Create v0.3.2 at the verified commit with English release notes. Verify the Release workflow succeeds and all four archives, four signatures, and checksums are present.
4. Download and verify the published Linux x64 artifact, check its version, and exercise self-update on an isolated copy of the previous release. Record the release URL and results.
5. If Docker publishing credentials are available, package the release through the existing Docker workflow and verify its result. Otherwise report that distribution limitation.

## Risks

Self-update cannot consume the release until the archives and signatures have been uploaded. Keep existing signing requirements enabled and verify published assets before reporting completion. Container image publishing depends on separately configured credentials.

## Scope

Startup-lock implementation and tests already approved; Cargo.toml, Cargo.lock, CHANGELOG.md, README version references, and release tracking. No unrelated refactors or dependency upgrades.

## Alternatives

Follow the existing signed release workflow instead of uploading ad hoc locally built binaries.

## Annotations

The user explicitly requested publishing the new version; no further approval is needed for this scope.
